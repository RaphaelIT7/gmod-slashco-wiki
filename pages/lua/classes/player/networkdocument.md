<function name="NetworkDocument" parent="Player" type="classfunc">
	<description>
		Sends a single document (with its rating) to the player over the network, or a deletion notice if the player no longer has it.
	</description>
	<realm>Server</realm>
	<group>Documents</group>
	<args>
		<arg name="name" type="string">Name of the document to (re-)network.</arg>
	</args>
</function>
