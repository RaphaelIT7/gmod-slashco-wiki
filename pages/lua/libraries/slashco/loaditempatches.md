<function name="LoadItemPatches" parent="SlashCo" type="libraryfunc">
	<description>
		<internal></internal>
		Includes every file in `lua/slashco/patch/effect/` and `lua/slashco/patch/item/`, which are meant to modify already-registered item/effect tables.<br>
		Called automatically on startup and whenever game content changes.
	</description>
	<realm>Shared</realm>
	<group>Items</group>
</function>
