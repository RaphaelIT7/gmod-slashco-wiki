<function name="SlamDoor" parent="Player" type="classfunc">
	<description>
		Tries to slam the given door open
	</description>
	<realm>Server</realm>
	<args>
		<arg name="door" type="Entity">The target Door entity (if not a door, this function won't do anything)</arg>
	</args>
</function>