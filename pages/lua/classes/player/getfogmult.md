<function name="GetFogMult" parent="Player" type="classfunc">
	<description>
		gets the fog multiplier that is for this player only.
		This function is created by the <page text="DTVar Networking System">DTVar Networking</page>
	</description>
	<realm>Shared</realm>
	<rets>
		<ret name="multiplier" type="number" default="0">fog multiplier. 1 = no change</ret>
	</rets>
</function>