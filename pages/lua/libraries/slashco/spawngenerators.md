<function name="SpawnGenerators" parent="SlashCo" type="libraryfunc">
	<description>
		Spawns the generators required for the current round.<br>
		Forced generator spawn points receive priority.<br>
		Each selected generator also receives a randomly selected battery spawn.<br>
		Aborts the round if no generator spawn entities are available.
	</description>
	<realm>Server</realm>
</function>