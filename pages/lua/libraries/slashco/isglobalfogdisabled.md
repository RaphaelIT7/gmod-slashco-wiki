<function name="IsGlobalFogDisabled" parent="SlashCo" type="libraryfunc">
	<description>
		Returns whether the global fog was disabled using <page>SlashCo.DisableGlobalFog</page>.<br>
		See the <page text="Fog">Fog</page> system for what disabling it does.
	</description>
	<realm>Shared</realm>
	<group>Fog</group>
	<rets>
		<ret name="disabled" type="boolean" default="false">Whether the global fog is disabled</ret>
	</rets>
</function>