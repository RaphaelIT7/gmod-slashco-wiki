<function name="LegacySetup" parent="SlashCo" type="libraryfunc">
	<description>
		Adds spawning entities from legacy map configuration files if they exist.<br>
		Legacy configuration data is converted into the corresponding modern spawn entities.
	</description>
	<realm>Server</realm>
</function>