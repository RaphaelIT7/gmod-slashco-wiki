<function name="DisableAlarmLights" parent="SlashCo" type="libraryfunc">
	<description>
		Reverses <page>SlashCo.EnableAlarmLights</page>: turns off all registered alarm cagelights, turns the non-alarm lights back on, disables every player's dynamic flashlight, and sets `GameData.IsBlackout` back to `false`.
	</description>
	<realm>Server</realm>
</function>
