<function name="SlashCo:DrawHUD" parent="" type="hook">
	<description>
		Called every frame from `PostDrawHUD` (once the local player is valid) inside a `cam.Start2D()`/`cam.End2D()` block.<br>
		Use it to draw custom 2D HUD elements, such as pings.<br>
	</description>
	<realm>Client</realm>
</function>
