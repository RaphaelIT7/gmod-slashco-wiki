<function name="Redo" parent="SlashCo.MapTools" type="libraryfunc">
	<description>
		Re-applies the last change that was reverted by <page>SlashCo.MapTools.Undo</page>
		<note>
			If you do any changes after calling <page>SlashCo.MapTools.Undo</page> without calling this function directly afterwards you cannot restore the changes again!
		</note>
	</description>
	<realm>Server</realm>
</function>