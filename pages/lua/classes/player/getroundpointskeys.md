<function name="GetRoundPointsKeys" parent="Player" type="classfunc">
	<description>
		Returns the round point categories the player currently has points recorded for.
	</description>
	<realm>Shared</realm>
	<group>Round Points</group>
	<rets>
		<ret name="keys" type="table<string>">The player's round point category keys, or an empty table if none are recorded.</ret>
	</rets>
</function>
