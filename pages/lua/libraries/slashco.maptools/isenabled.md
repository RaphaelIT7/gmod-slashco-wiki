<function name="IsEnabled" parent="SlashCo.MapTools" type="libraryfunc">
	<description>
		Returns if the Map tools are enabled or not.
	</description>
	<realm>Shared</realm>
	<rets>
		<ret name="enabled" type="boolean">`true` if their enabled</ret>
	</rets>
</function>