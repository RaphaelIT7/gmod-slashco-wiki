<function name="SpawnDocuments" parent="SlashCo" type="libraryfunc">
	<description>
		Spawns the required documents for the current round.<br>
		The number of documents is based on the required document count plus the map size.<br>
		If no document spawn entities are available, document spawning is marked as failed.
	</description>
	<realm>Server</realm>
</function>