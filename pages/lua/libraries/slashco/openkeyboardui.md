<function name="OpenKeyboardUI" parent="SlashCo" type="libraryfunc">
	<description>
		Opens (or re-opens) the keybinds menu, listing every entry in `SlashCo.KeyboardBinds` and letting the player rebind each one by clicking it and pressing a new key.<br>
		Bindings are saved via `SlashCo.SaveKeyboardBinds` once the menu is closed.
	</description>
	<realm>Client</realm>
	<group>Keyboard</group>
</function>