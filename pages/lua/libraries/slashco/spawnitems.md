<function name="SpawnItems" parent="SlashCo" type="libraryfunc">
	<description>
		Spawns items for the current round.<br>
		World props configured to replace items are converted into their corresponding items.<br>
		Slasher-specific items, the beacon, and randomly selected items are then spawned.
	</description>
	<realm>Server</realm>
</function>