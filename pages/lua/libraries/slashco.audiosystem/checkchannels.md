<function name="CheckChannels" parent="SlashCo.AudioSystem" type="libraryfunc">
	<description>
		Checks all existing channels and removes any from the internal table if they are invalid.<br>
		This function is simply a cleanup function to get rid of destroyed channels.<br>
		<internal></internal>
	</description>
	<realm>Client</realm>
</function>