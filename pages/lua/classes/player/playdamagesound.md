<function name="PlayDamageSound" parent="Player" type="classfunc">
	<description>
		Plays a random damage sound effect at the player's position using <page>SlashCo.AudioSystem.PlaySound</page>.
	</description>
	<realm>Shared</realm>
	<group>Damage</group>
	<args>
		<arg name="additionalRange" type="number" optional>Extra distance added to the sound's min/max audible range. Defaults to `0`.</arg>
	</args>
</function>
